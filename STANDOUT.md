# Lab 3 Standout Notes

This submission focuses on making the Lab 2 minutes page look intentional and readable on different screen sizes, while covering the full CSS checklist.

What I emphasized:

- A cleaner visual hierarchy for meeting minutes (header, cards, quick links, and grouped sections)
- Responsive layout behavior for phone, tablet, and desktop views
- Required selector/combinator usage, including newer selectors (`:has()` and nesting)
- Flexbox and grid examples with multiple child elements
- Color/spacing/font improvements without overcomplicating the structure

Files used for the lab:

- `index.html`
- `styles.css`
- `screenshots/Screenshot (9).png` (validator screenshot)

Final check:

- External CSS is the primary styling method
- One inline style and one `<style>` block are included
- CSS validates with expected warnings for newer syntax support
